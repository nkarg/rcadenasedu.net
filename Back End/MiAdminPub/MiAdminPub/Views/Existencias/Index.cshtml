@model IEnumerable<MiAdminPub.Models.Existencia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cantidad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cantidad)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ExistenciaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ExistenciaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ExistenciaId })
        </td>
    </tr>
}

</table>
